# DFS-N × 偏在報酬 実験タブ 設計メモ

「探索優先順位を偏らせた DFS（DFS-N）は、報酬が偏在する領域と方位が一致すると
BFS より少ない探索コストで報酬を回収できる」という仮説を、ブラウザ上で再現・
可視化・統計検証するための実験タブの設計をまとめる。

---

## 1. 目的（検証したい 3 つの主張）

| # | 主張 | 対応セクション |
|---|------|----------------|
| H1 | 報酬が偏在するとき、方位バイアス付き DFS（DFS-N）は BFS より早く報酬を回収する | 単発実行・回収曲線 |
| H2 | H1 の優位は **バイアス方位と報酬領域方位の一致度**に依存する（不一致なら劣化する） | バイアス角スイープ |
| H3 | 同じ効果は「未知迷路を走るマイクロマウス型」でも成立する。さらに、大まかなゴール座標だけを事前情報として与えると、ゴール到達歩数が短縮される | マイクロマウス検証 |

H1/H2 は**全知探索**（迷路が既知）の問題。H3 は**未知環境の実移動**の問題であり、
コスト単位が異なる（下記 3 節）。

---

## 2. 用語と記号

- 迷路: 1 階層（2D）に固定。`state.layers[0][y][x]`（0=通路, 1=壁）。
- 座標系: x 右向き、y **下向き**（Canvas 準拠）。
- 方位角 θ: **数学慣習**（0°=右, 90°=上, 反時計回り）で UI に出す。
  内部のグリッドベクトルへは `b(θ) = (cos θ, −sin θ)` で変換する。
- 報酬領域 R: 中心 `(cx, cy)`・半径 `r` の円。UI では迷路サイズに対する百分率で指定。
- 報酬集合 W: 通路セルの部分集合。`|W| = K`。
- 集中率 c: W のうち R 内に置く割合（c=1.0 で完全偏在, c=0 で一様＝対照条件）。

---

## 3. コスト単位（ここを混同しない）

| 実験 | コスト単位 | 理由 |
|------|-----------|------|
| H1 / H2（全知探索） | **展開セル数**（フロンティアから取り出した回数） | BFS と DFS で共通に定義できる標準的な指標。BFS の展開順は単一エージェントの移動として物理的に実現不能なので、歩数では比較できない |
| H3（マイクロマウス） | **実移動歩数** | 実機ロボットのコストはあくまで走行距離。センサで見えただけの報酬は回収扱いにしない（「通過することで得られる報酬」の定義に従う） |

この非対称性は UI 上にも明記する。

---

## 4. アルゴリズム

いずれも「セルを 1 つずつ展開する」列（展開順 `order[]`）を返す。

### 4.1 BFS（対照）
FIFO。展開順＝スタートからの距離順。方位バイアスなし（等方）。

### 4.2 DFS（通常）
LIFO。近傍の優先順位は固定（上→右→下→左）。バイアスなしの深さ優先対照。

### 4.3 DFS-N（提案）
LIFO。近傍 `n` の優先度を、進行ベクトル `d = n − cur` とバイアス `b(θ)` の
内積で決める。

```
rank(n) = −( d̂ · b(θ) ) + ε · dirIndex(n)      # rank 小 = 優先
```

スタックには rank 降順に push するため、最も整合する方向が最初に pop される。
`ε·dirIndex` は同点時の決定性を保つためのタイブレーク。

θ を持たない通常 DFS は「rank = dirIndex」の特殊形であり、DFS-N は DFS の
真の一般化になっている。

### 4.4 Greedy-領域（参考上限）
優先度付きキューによる**重み付き最良優先探索**。DFS-N と同じ事前情報（領域の
中心 `c` と半径 `r`）だけを、方位バイアスではなく距離場として直接使ったときの
参考線。**個々の報酬セルの座標は参照しない**（参照すれば真の上界になるが、
DFS-N と情報量が揃わず比較として成立しない）。**提案手法より強いことがある**が、
それを隠さず併記する。

```
h(n) = d(n, c)                    # 領域の使い方 = 中心
     = max(0, d(n, c) − r)        # 領域の使い方 = 縁（R の内側は 0）
g(n) = 発見時の探索木の深さ（スタートからの辺数）
f(n) = (1 − w)·g(n) + w·h(n) + ε·U(0,1),  ε = 1e−6   # f 小 = 先に展開
```

各セルは 1 回だけキューに入り、再展開（decrease-key）はしない。コスト単位が
展開順であり、同じセルを二度数えないため。

| パラメータ | 値域 | 既定 | 意味 |
| --- | --- | --- | --- |
| 貪欲度 `w` | 0–1 | 1.00 | `w=1` は純粋な貪欲最良優先、`w=0` は一様コスト探索＝**BFS と一致**。BFS と Greedy-領域は同族の両端であり、`w` は「事前情報をどれだけ強く使うか」の連続軸になる |
| 距離 `d` | L1 / L2 / L∞ | L1 | 4 近傍格子ではグラフ距離が L1 に一致（無矛盾）。L2・L∞ は楽観的で等距離集合の形が変わる。ヒューリスティック形状の感度分析用 |
| 領域の使い方 | 中心 / 縁 | 中心 | 「縁」は 4.5 の λ 項と同形の台地型。`w=1` だと R 内が同点になり順序が乱数任せになる点に注意 |
| 同点解消 `ε` | 1e−6 固定 | — | 格子上は等距離セルが大量に生じる。固定順で解くと近傍列挙順（＝方位）が参考上限側に混入するため、試行シードで再現可能な一様乱数で解く（DFS・DFS-N と同じ扱い） |

nAUC 等の数値を報告するときは `w`・距離・領域の使い方を必ず併記する
（UI の表1・表3 の行見出しと、スイープ CSV 先頭の `#` メタ行に出力される）。

### 4.5 マイクロマウス型（フロンティア探索）
既知/未知を持つ占有格子。未知セルは「通行可能」と楽観視する（標準的なフラッド
フィル法と同じ）。1 歩ごとに視界（`computeVisibleCells`: 直進 LOS ＋廊下の壁）
で既知領域を更新する。

次に向かうフロンティア（未知セル）を、

```
score(cell) = dist(pos → cell) + λ · max(0, |cell − center| − r)
```

で選ぶ。

- `λ = 0`（無情報）: 最近傍フロンティア探索。従来のマイクロマウス。
- `λ > 0`（領域誘導）: 「ゴールは領域 R のあたり」という事前情報を、領域外への
  ペナルティとして距離場に埋め込む。R の内側はペナルティ 0。

再計画は「目標が既知化した」「次の 1 歩が既知の壁だった」ときのみ行う（毎歩の
全域 BFS を避ける）。

---

## 5. 評価指標

展開順 `order[]`（または実移動列）と報酬集合 W から回収曲線を作る。

- 回収率 `c(n) = |W ∩ order[0..n)| / |W|`
- **nAUC** `= (1/N) Σ c(n)` ∈ [0,1]。**大きいほど早く回収**。主指標。
- `cost@50%`, `cost@90%`: 回収率が 50% / 90% に達する n（全セル数で正規化）
- `cost@first`: 最初の 1 個を回収した n

nAUC を主指標にする理由: 「50% 到達」等の単一しきい値は報酬配置のゆらぎに弱いが、
nAUC は曲線全体を積分するため分散が小さく、複数シードでの平均比較に向く。

---

## 6. 実験デザイン

### 6.1 単発（可視化用）
1 つの迷路・1 つの報酬配置に対し、BFS / DFS / DFS-N(θ) / Greedy を実行し、
回収曲線を重ね描き＋メトリクス表を出す。迷路上には探索順ヒートマップ・報酬・
領域円を重ねる。

### 6.2 バイアス角スイープ（H2 の主図）
θ を 0°〜330°（12 点）で振り、各 θ につき M 個のシードで DFS-N を実行し
nAUC の平均±標準偏差を得る。**極座標（レーダー）チャート**に描き、

- BFS の nAUC を等方な基準円として重ねる
- 報酬領域の実方位を矢印で示す

ことで「バイアスが領域方位に一致した角度で BFS の円を突き抜ける」形が一目で
分かるようにする。

### 6.3 マイクロマウス検証（H3）
同じ M シードで、実移動歩数を単位に

- 報酬回収: `λ=0` vs `λ>0` の nAUC / cost@50% / cost@90%
- ゴール到達: `λ=0` vs `λ>0` の到達歩数

を比較する。ゴールは既定で領域内に置く。**「ゴール配置: 領域外（事前情報が誤り）」**
も選べるようにし、事前情報が外れたときのコスト（誘導が裏目に出る）も示せるように
する。

### 6.4 対照条件
集中率 `c` を 0 にすると報酬は一様分布になり、DFS-N の優位が消えることを確認できる。
これが「効果は偏在に由来する」ことの内的妥当性チェックになる。

---

## 7. 実装方針

### 7.1 ファイル分割
- `lab.js` を新設（`app.js` は 3100 行あり、これ以上の肥大を避ける）。
- `app.js` の内部関数はブラウザではグローバル字句環境経由で、Node では
  `require("./app.js")` 経由で受け取る（同一コードで両対応）。
- `index.html` をタブ化し、既存 UI は `#panel-viz`、新機能は `#panel-lab` に入れる。
  既存 UI の DOM 構造・ID は変更しない。

### 7.2 迷路 state の扱い
探索関数群（`isOpen` / `getNeighbors` 等）はグローバル `state` を読むため、
ラボは自前の迷路スナップショットを持ち、計算の前後で `state` を退避・復元する
（既存のバッチ検証が `restoreDisplayMaze` で行っているのと同じ方針）。

### 7.3 再現性
迷路シード・報酬配置シードを分離し、どちらも UI で指定・表示する。
同一シードなら迷路も報酬配置も完全に再現される。

### 7.4 検証
`test/lab.test.js` を追加し、Node 標準テストランナーで

- DFS-N の近傍順序がバイアス方位に従うこと
- バイアスが領域方位と一致したとき nAUC が不一致時より大きいこと
- nAUC・cost@k の算術が定義どおりであること
- 集中率 0（一様配置）では DFS-N と BFS の nAUC 差が縮むこと
- マイクロマウスが探索を完了し、領域誘導ありでゴール到達歩数が短縮されること

を検証する。

---

## 8. 想定される反証・限界（UI にも明記する）

- DFS-N は**最短経路を保証しない**。本実験は「報酬回収の早さ」のみを問う。
- Greedy-領域のように領域情報をより直接使う手法が DFS-N を上回りうる。
  DFS-N の主張は「BFS より良い」であって「最良である」ではない。
- 事前情報（領域）が誤っている場合、誘導は逆効果になる。6.3 で明示的に測る。
- コスト単位が全知探索（展開数）とマイクロマウス（歩数）で異なるため、
  両者の数値を直接比較してはならない。
