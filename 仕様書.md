# 迷路ゲーム / 迷路ソルバー仕様書

## 1. 概要
本プロジェクトは「多階層の迷路を自動生成し、複数の解法アルゴリズムが解く様子を可視化する」アプリケーションである。

現在は次の 2 つの実装を持つ。

- **ブラウザ版（メイン）**: `index.html` / `styles.css` / `app.js`。GitHub Pages で公開する静的 Web アプリ。
- **Python 版（レガシー）**: `maze_game.py`。Tkinter による初期実装。参考用に保持する。

公開 URL: https://ry32767.github.io/MazeCreator/

---

## 2. ブラウザ版の機能

画面はタブで 2 つに分かれる。

- **迷路ソルバー**（`#panelViz`）: 2.1〜2.6 の従来機能。
- **DFS-N 研究ラボ**（`#panelLab`）: 2.7 の実験環境。

タブは `role="tablist"` / `role="tab"` / `role="tabpanel"` で構成し、左右矢印キーでも切り替えられる。

### 2.1. 迷路の自動生成
- 起動時および「生成」ボタンで、指定サイズ・指定階層数の迷路を生成する。
- 生成アルゴリズムは穴掘り法（深さ優先探索）。各階層は必ず全通路が連結した完全迷路になる。
- 階層が複数ある場合は、上下階の同一座標が通路同士の地点に「階段」を配置し、階をまたぐ移動を可能にする。
- スタートとゴールは BFS による最遠点探索で決め、スタートは最下階・ゴールは最上階に置く。ゴールは行き止まりになるよう調整する。
- 必ずスタートからゴールまで到達可能な経路を 1 つ以上持つ。

### 2.2. 表示
- Canvas に迷路を描画する。壁・通路・探索済み・経路などを色分けする。
- スタート（S）・ゴール（G）・階段（上り▲／下り▼／両方↕）は、セルが小さくても視認できるよう大きな円形マーカーで強調する。導き出した正解ルートは、暗い縁取り付きの太い線で連続的に描く。
- 表示モードを「全階（平面・縦並び）」「全階（3D俯瞰）」「1階ずつ」から選べる。複数階では既定で全階を同時表示するため、アニメーション中に階を行き来しても表示が切り替わらず追いやすい。
    - **全階（平面）**: 各階を縦に並べて真上から表示。
    - **全階（3D俯瞰）**: 各階を斜めから見たオブリーク投影で積層表示し、階段は上下階をつなぐ縦の柱で立体的に描く。
    - **1階ずつ**: 従来表示。「下階／上階」ボタンまたは PageUp / PageDown で階を切り替える。

### 2.3. 解法アルゴリズム（可視化）
次の 6 種類を選択して実行でき、探索の進行をアニメーション表示する。
- Paintbrush（塗りつぶし＋連結性判定による経路確定）
- 右手法 / 左手法（壁伝い）
- BFS（幅優先探索、最短経路を保証）
- DFS（深さ優先探索）
- A*（マンハッタン距離 + 階層差を許容的ヒューリスティックとし、最短経路を保証）

実行中の探索セル数・経路長・所要時間をメトリクスとして表示する。

### 2.4. 手動プレイ
- 矢印キー / WASD、または画面の方向パッドでプレイヤーを操作する。
- 壁は通り抜けられない。階段地点では自動で階を移動する。
- ゴール到達時に「ゴール」と表示する。

### 2.5. 研究・実験向け機能
- **シード指定**: 擬似乱数（mulberry32）にシードを与えることで、同じ設定なら同一の迷路を再現できる。空欄ならランダムなシードを割り当て、使用したシードを表示する。同一迷路上で複数アルゴリズムを公平に比較するための土台。
- **ループ率（braiding）**: 完全迷路の行き止まりを一定割合で開放し、別解（ループ）を作る。0% は唯一解、値を上げるほど壁伝い法が解けなくなり BFS/A* との差が際立つ。
- **階段密度 / 生成試行回数**: 階のつなぎ目あたりの階段数、最長経路を選ぶための生成試行回数を調整できる。
- **全解法比較**: 現在の迷路上で全解法を即時実行し、経路長・探索セル数・所要時間・最短一致を一覧表で比較する。
- **バッチ検証**: 現在の設定でランダムな迷路を複数生成し、各解法の平均経路長・平均探索セル数・最短率・平均所要時間を集計する。単一の迷路に依存しない統計的な比較ができる。処理は時間予算ごとに分割（非同期チャンク化）して実行し、進捗バーを表示するため、大きい迷路・多数の試行でも画面が固まらない。停止ボタンで中断できる。
- **探索ヒートマップ**: 解法の探索済みセルを「探索された順番」のグラデーション（寒色→暖色）で塗り分け、BFS の一様拡散と A*/DFS の偏った探索の違いを視覚化する。
- **パラメータ掃引**: ループ率または階段密度を横軸に振りながら、各解法の平均探索セル数を折れ線グラフ（縦軸）で比較する。パラメータが探索効率へ与える影響を一目で把握できる。
- **結果エクスポート**: バッチ検証・パラメータ掃引の結果を CSV / JSON で書き出し、外部ツール（表計算・解析スクリプト等）で再分析できる。JSON には生成設定のメタ情報を含む。

### 2.7. DFS-N 研究ラボ（実験タブ）

「探索優先順位を方位で偏らせた深さ優先探索（DFS-N）は、報酬が偏在する領域と方位が一致するとき
BFS より少ない探索コストで報酬を回収できるか」を検証する。迷路は 1 階層（2D）固定。
設計の詳細・指標の定義・限界は `dfsn_experiment_plan.md` を参照。

#### 検証する主張
- **H1**: 報酬が偏在するとき、方位が一致した DFS-N は BFS より早く報酬を回収する。
- **H2**: その優位はバイアス方位と報酬領域方位の一致度に依存する（不一致なら劣化する）。
- **H3**: 同じ効果はマイクロマウス型（未知環境）でも成立し、さらに大まかなゴール座標だけを
  事前情報として与えるとゴール到達歩数が短縮される。

#### 報酬の配置
中心 (x, y)・半径 r の円領域を指定し、集中率の割合をその領域内に、残りを領域外へ一様に置く。
集中率 0% は一様分布であり、効果が偏在に由来することを確かめる対照条件になる。
中心は数値指定のほか、プリセット（右上・右下など）と迷路のクリックでも設定できる。

#### アルゴリズム
- **BFS**: 幅優先の対照（等方）。
- **DFS（等方・順序ランダム）**: 近傍の順序を毎回ランダムに引く深さ優先の対照。
  「上→右→下→左」のような固定順の DFS はそれ自体が右上方向のバイアスを持つため、
  方位効果の分離には使えない。
- **DFS-N**: `rank(n) = −(d̂ · b(θ)) + ε·U(0,1)` で近傍の優先順位を決める。θ は数学慣習
  （0°=右、90°=上、反時計回り）。ε 項は 45° の倍数で生じる厳密な同点の解消にのみ効く。
- **Greedy-領域**: 領域中心へのマンハッタン距離を優先度とする最良優先探索。DFS-N と同じ
  事前情報を使い切った場合の参考上限であり、DFS-N を上回ることがある。

#### 指標
展開順（マイクロマウスは実移動列）と報酬集合から回収曲線 `c(n)` を作る。

- **nAUC** `= (1/H) Σ c(n)` ∈ [0,1]。大きいほど早く回収。主指標。
- `cost@first` / `cost@50%` / `cost@90%`。

同じセルを複数回通っても報酬は 1 回しか回収しない（マイクロマウスは引き返しで同じセルを踏む）。

#### コスト単位
全知探索（BFS/DFS/DFS-N/Greedy）は**展開セル数**、マイクロマウスは**実移動歩数**。
BFS の展開順は単一エージェントの移動として物理的に実現できないため、両者の数値を
直接比較してはならない。この非対称性は UI にも明記する。

#### 図表
- **図1 回収曲線**: 探索コスト対回収率。4 手法を重ね描き。
- **図2 バイアス角スイープ**: 22.5° 刻み 16 方位の nAUC を極座標に描き、BFS の等方基準円と
  報酬領域の実方位を重ねる。動径軸は 0 始まりではないため、リングに実値を表示する。
  4 近傍格子では近傍の並び順が 45° ごとにしか変わらないので、曲線は階段状になる。
- **図3 マイクロマウス比較**: 無情報（λ=0）と領域誘導（λ>0）の棒グラフ。群ごとに単位が
  異なるため群内最大値で正規化し、誤差棒（±sd）を付す。
- **表**: 平均±sd に加えて中央値と「シードごとの勝率」を出す。歩数の分布は右に長く尾を引くため、
  平均だけでは少数の大勝ちに引きずられる。

#### マイクロマウス（H3）
未知セルを通行可能と楽観視するフラッドフィル法。次に向かう目標を
`score(cell) = dist(pos → cell) + λ·max(0, |cell − center| − r)` で選ぶ。λ=0 が従来の
最近傍フロンティア探索、λ>0 が「ゴールは領域 R のあたり」という事前情報を距離場に
埋め込んだ領域誘導。

- 報酬回収は**被覆課題**（まだ踏んでいないセルを目標にし、到達可能な全通路を踏む）。
- ゴール到達は**探索課題**（まだ見ていないセルを目標にし、ゴールが視界に入ったらそこへ向かう）。
- ゴール配置は「領域内（事前情報が正しい）」と「領域外（事前情報が誤り）」を選べ、
  誘導が裏目に出る場合のコストも測れる。

#### 再現性
迷路シードと報酬配置シードを分離し、どちらも同一なら迷路も配置も完全に再現される。
探索関数群は共有 `state` を読むため、ラボは自前の迷路スナップショットを持ち、計算の前後で
`state` を退避・復元する。

### 2.6. アクセシビリティ
- 操作状況は `aria-live` のステータス領域に反映する。
- 解法完了・生成・比較の結果は、スクリーンリーダー向けの詳細アナウンス領域（経路長・最短との差・探索セル数・所要時間など）に文章で出力する。
- 生成オプションには補足説明（`aria-describedby`）を付与している。

---

## 3. 技術仕様

### 3.1. ブラウザ版
- 使用言語: JavaScript（フレームワーク・ビルド不要）
- 描画: HTML5 Canvas（研究ラボの図は devicePixelRatio を考慮し、高さは `aspect-ratio` で
  与えて狭い画面でも縦横比が崩れないようにする）
- データ構造:
    - 迷路: 階層ごとの 2 次元配列（0=通路, 1=壁）の配列 `layers[z][y][x]`
    - 階段: 座標キー `"x,y,z"` から接続先配列への `Map`
    - 座標: `{ x, y, z }` オブジェクト
- テスト: Node.js 標準テストランナー（`npm test`）。`app.js` / `lab.js` は Node からも読み込め、DOM 非依存のアルゴリズム関数を検証できる。`lab.js` はブラウザではグローバル字句環境から、Node では `require("./app.js")` から依存を受け取る。

### 3.2. Python 版（レガシー）
- 使用言語: Python 3 / `tkinter`
- 迷路生成: 穴掘り法（深さ優先探索）
- 操作: 矢印キー

---

## 4. ファイル構成
```
MazeCreator/
├── index.html              # ブラウザ版エントリ
├── styles.css              # スタイル
├── app.js                  # 迷路生成・解法アルゴリズム（ブラウザ／Node 共用）
├── lab.js                  # DFS-N 研究ラボ（実験ロジック・図表描画／Node 共用）
├── test/maze.test.js       # 単体テスト
├── test/lab.test.js        # DFS-N ラボの単体テスト・仮説検証テスト
├── maze_game.py            # Python(Tkinter) 版（レガシー）
├── 仕様書.md               # 本ドキュメント
├── AGENTS.md               # エージェント向けガイド
├── maze_algorithm_plan.md  # 生成アルゴリズム設計メモ
├── dfsn_experiment_plan.md # DFS-N 実験の設計メモ（目的・指標・限界）
└── paintbrush_solver_spec.md # Paintbrush 解法の設計メモ
```
